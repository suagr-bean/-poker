package service


func GameStartText(){
	/*
	game:=GameFactory("texas")
	_=game.MakeJudge()
    dealer:=game.MakeDealer()
     d,T:=dealer.NewDeck()//T是发牌员 d 是deck的属性
	g:=model.NewGameData(6)
	g.Add(0,[]int{15,10})
    g.Add(1,[]int{25,30})
    playerall:=g.GetAllPlayer()
	fmt.Println(playerall[1])
	
	T.ShuffleCard(d)
	_=T.Dealing(1,d)
     T.Known([]int{25,30},d)
	 fmt.Println(d.Knowns)
	 fmt.Println(d.AllCards)
	b:= model.NewBoard([]int{30,25,18})
    board:=b.GetBoardCards()
	fmt.Println(board)
	utils.DealCount([]int{8,9})
	 utils.Deal(g,board)
	result:=b.ProcessCard(0,[]int{18,30})
	fmt.Println(result)
	*/
	
}